# Source Mirror

This folder contains a legacy/source-layout mirror kept for compatibility.

## Contents

- `neo_trader/features/` - mirrored feature modules.

## Rules

- The canonical package is `neo_trader/` at the repository root.
- Keep mirrored files aligned only when compatibility requires it.
- Do not add new runtime systems here unless the project fully migrates to a `src/` layout.

